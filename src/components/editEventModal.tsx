"use client";

import React from 'react'

import {
    Card,
    CardContent,
    CardDescription,
    CardFooter,
    CardHeader,
    CardTitle,
} from "@/components/ui/card"

export default function editEventModal() {
  return (
    <div className="flex items-center justify-center h-screen">
        <Card>
            <CardHeader>Hello this is a pop up</CardHeader>
        </Card>
    </div>
  )
}
